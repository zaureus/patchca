<?xml version="1.0"?>

<project default="jar" basedir=".">
	<property name="src.dir" value="${basedir}/src" />
	<property name="build.dir" value="${basedir}/build" />
	<property name="build.classes.dir" value="${build.dir}/classes" />
	<property name="version.num" value="0.5.0"/>

	<target name="compile">
		<mkdir dir="${build.classes.dir}" />
		<javac srcdir="${src.dir}" destdir="${build.classes.dir}" debug="on" deprecation="on" optimize="off" />
	</target>

	<target name="jar" depends="compile">
		<delete file="${build.dir}/patcha${version.num}.jar"/>
		<delete file="${build.dir}/MANIFEST.MF"/>
		<manifest file="${build.dir}/MANIFEST.MF">
			<attribute name="Implementation-Version" value="${version.num}"/> 
		</manifest>
		<jar basedir="${build.dir}" destfile="${build.dir}/patcha${version.num}.jar" includes="**/*.class" manifest="${build.dir}/MANIFEST.MF">
		</jar>
	</target>
	
</project>
